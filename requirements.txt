# Dependencias del RPA de Solicitudes de Pago (SIPP)
# Sin versión fija: usa la última, que trae binarios para Python 3.14.
playwright
openpyxl     # lectura de bases de datos en Excel (.xlsx)
pymupdf      # comprimir PDFs > 10MB y OCR de PDFs (PyMuPDF / fitz)

# --- OCR (Extractor Bancario unificado) ---
pytesseract  # motor OCR (requiere Tesseract instalado / empaquetado)
Pillow       # manejo de imágenes
pypdf        # leer/separar PDFs
pillow-heif  # imágenes HEIC/HEIF (fotos de iPhone)
